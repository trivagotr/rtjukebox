# React Native 0.73 → 0.76 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade React Native from 0.73.0 to 0.76.9 while keeping the app fully functional on Android.

**Architecture:** Update package.json versions, Android Gradle config, iOS Podfile, and remove one deprecated API call. New Architecture stays disabled (`newArchEnabled=false` already set). No screen logic changes.

**Tech Stack:** React Native 0.76.9, React 18.3.2, Gradle 8.6, AGP 8.3.2, Kotlin 1.9.24, NDK 26.1.10909125

---

## File Map

| File | Change |
|---|---|
| `mobile/package.json` | Bump RN, React, @react-native/* packages, track-player |
| `mobile/android/build.gradle` | Kotlin 1.8.0→1.9.24, AGP 8.1.4→8.3.2 |
| `mobile/android/gradle/wrapper/gradle-wrapper.properties` | Gradle 8.3→8.6 |
| `mobile/android/app/build.gradle` | NDK version, remove flipper-integration dep |
| `mobile/ios/Podfile` | Remove Flipper config |
| `mobile/src/screens/RadioScreen.tsx` | Remove UIManager import + usage |

> `android/gradle.properties` already has `newArchEnabled=false` — no change needed.

---

### Task 1: Update package.json

**Files:**
- Modify: `mobile/package.json`

- [ ] **Step 1: Edit package.json — bump all target versions**

Open `mobile/package.json` and replace the `dependencies` and `devDependencies` sections:

```json
{
  "name": "com.radiotedumobile",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "lint": "eslint .",
    "start": "react-native start",
    "test": "jest"
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "^1.21.0",
    "@react-navigation/bottom-tabs": "^6.5.20",
    "@react-navigation/native": "^6.1.18",
    "@react-navigation/native-stack": "^6.9.26",
    "axios": "^1.13.2",
    "react": "18.3.2",
    "react-native": "0.76.9",
    "react-native-image-picker": "^8.2.1",
    "react-native-rss-parser": "^1.5.1",
    "react-native-safe-area-context": "^4.10.1",
    "react-native-screens": "^3.29.0",
    "react-native-track-player": "^4.1.1",
    "react-native-vector-icons": "^10.3.0",
    "socket.io-client": "^4.8.3",
    "zustand": "^5.0.10"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "@babel/preset-env": "^7.20.0",
    "@babel/runtime": "^7.20.0",
    "@react-native/babel-preset": "^0.76.0",
    "@react-native/eslint-config": "^0.76.0",
    "@react-native/metro-config": "^0.76.0",
    "@react-native/typescript-config": "^0.76.0",
    "@types/react": "^18.3.0",
    "@types/react-native-vector-icons": "^6.4.18",
    "@types/react-test-renderer": "^18.3.0",
    "babel-jest": "^29.6.3",
    "eslint": "^8.19.0",
    "jest": "^29.6.3",
    "prettier": "2.8.8",
    "react-test-renderer": "18.3.2",
    "typescript": "5.0.4"
  },
  "engines": {
    "node": ">=18"
  }
}
```

- [ ] **Step 2: Commit**

```bash
cd E:\rtmusicbox
git add mobile/package.json
git commit -m "chore(mobile): bump RN to 0.76.9, React to 18.3.2, update @react-native/* packages"
```

---

### Task 2: Update Android Gradle config

**Files:**
- Modify: `mobile/android/build.gradle`
- Modify: `mobile/android/gradle/wrapper/gradle-wrapper.properties`
- Modify: `mobile/android/app/build.gradle`

- [ ] **Step 1: Update android/build.gradle — Kotlin and AGP versions**

In `mobile/android/build.gradle`, change:

```groovy
// BEFORE
        kotlinVersion = "1.8.0"
// ...
        classpath("com.android.tools.build:gradle:8.1.4")
```

To:

```groovy
// AFTER
        kotlinVersion = "1.9.24"
// ...
        classpath("com.android.tools.build:gradle:8.3.2")
```

Full resulting `ext` block:

```groovy
buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 21
        compileSdkVersion = 34
        targetSdkVersion = 34
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.9.24"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.3.2")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

allprojects {
    tasks.withType(JavaCompile).configureEach {
        options.release = 17
    }
}

apply plugin: "com.facebook.react.rootproject"
```

- [ ] **Step 2: Update gradle-wrapper.properties — Gradle 8.6**

In `mobile/android/gradle/wrapper/gradle-wrapper.properties`, change:

```properties
# BEFORE
distributionUrl=https\://services.gradle.org/distributions/gradle-8.3-all.zip
```

To:

```properties
# AFTER
distributionUrl=https\://services.gradle.org/distributions/gradle-8.6-all.zip
```

- [ ] **Step 3: Update android/app/build.gradle — remove flipper-integration**

In `mobile/android/app/build.gradle`, in the `dependencies` block, remove this line:

```groovy
    implementation("com.facebook.react:flipper-integration")
```

Final `dependencies` block should look like:

```groovy
dependencies {
    // The version of react-native is set by the React Native Gradle Plugin
    implementation("com.facebook.react:react-android")

    if (hermesEnabled.toBoolean()) {
        implementation("com.facebook.react:hermes-android")
    } else {
        implementation jscFlavor
    }

    implementation("androidx.car.app:app:1.4.0")
    implementation("androidx.media:media:1.7.0")
}
```

- [ ] **Step 4: Commit**

```bash
cd E:\rtmusicbox
git add mobile/android/build.gradle mobile/android/gradle/wrapper/gradle-wrapper.properties mobile/android/app/build.gradle
git commit -m "chore(mobile/android): Kotlin 1.9.24, AGP 8.3.2, Gradle 8.6, NDK 26.1, remove flipper"
```

---

### Task 3: Update iOS Podfile

**Files:**
- Modify: `mobile/ios/Podfile`

- [ ] **Step 1: Remove Flipper from Podfile**

Replace the entire `mobile/ios/Podfile` with:

```ruby
# Resolve react_native_pods.rb with node to allow for hoisting
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

platform :ios, min_ios_version_supported
prepare_react_native_project!

linkage = ENV['USE_FRAMEWORKS']
if linkage != nil
  Pod::UI.puts "Configuring Pod with #{linkage}ally linked Frameworks".green
  use_frameworks! :linkage => linkage.to_sym
end

target 'RadioTEDUMobile' do
  config = use_native_modules!

  use_react_native!(
    :path => config[:reactNativePath],
    # An absolute path to your application root.
    :app_path => "#{Pod::Config.instance.installation_root}/.."
  )

  target 'RadioTEDUMobileTests' do
    inherit! :complete
    # Pods for testing
  end

  post_install do |installer|
    # https://github.com/facebook/react-native/blob/main/packages/react-native/scripts/react_native_pods.rb#L197-L202
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false
    )
  end
end
```

Changes from previous version:
- Removed `flipper_config = ENV['NO_FLIPPER'] == "1" ? FlipperConfiguration.disabled : FlipperConfiguration.enabled`
- Removed `:flipper_configuration => flipper_config,` from `use_react_native!`

- [ ] **Step 2: Commit**

```bash
cd E:\rtmusicbox
git add mobile/ios/Podfile
git commit -m "chore(mobile/ios): remove Flipper from Podfile (deprecated since RN 0.74)"
```

---

### Task 4: Fix RadioScreen.tsx — remove deprecated UIManager usage

**Files:**
- Modify: `mobile/src/screens/RadioScreen.tsx`

- [ ] **Step 1: Remove UIManager from import**

In `mobile/src/screens/RadioScreen.tsx`, find the `react-native` import block (lines ~1-17) and remove `UIManager` and `Platform` from the destructured imports (Platform is only used for the UIManager call — verify no other Platform usage in the file before removing it).

Check if `Platform` is used anywhere else:

```bash
grep -n "Platform" E:/rtmusicbox/mobile/src/screens/RadioScreen.tsx
```

If `Platform` appears **only** in the UIManager block, remove both. If it's used elsewhere, keep `Platform` and remove only `UIManager`.

**If Platform is used elsewhere**, update the import to:

```tsx
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ActivityIndicator,
  FlatList,
  Image,
  ImageBackground,
  LayoutAnimation,
  Platform,
  useWindowDimensions,
  ScrollView,
  Modal,
} from 'react-native';
```

**If Platform is NOT used elsewhere**, update the import to:

```tsx
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ActivityIndicator,
  FlatList,
  Image,
  ImageBackground,
  LayoutAnimation,
  useWindowDimensions,
  ScrollView,
  Modal,
} from 'react-native';
```

- [ ] **Step 2: Remove the UIManager useEffect block**

Find and delete this entire block (around line 77-84):

```tsx
  useEffect(() => {
    if (Platform.OS === 'android') {
      if (UIManager.setLayoutAnimationEnabledExperimental) {
        UIManager.setLayoutAnimationEnabledExperimental(true);
      }
    }
  }, []);
```

- [ ] **Step 3: Commit**

```bash
cd E:\rtmusicbox
git add mobile/src/screens/RadioScreen.tsx
git commit -m "fix(mobile): remove deprecated UIManager.setLayoutAnimationEnabledExperimental (not needed in RN 0.76)"
```

---

### Task 5: Clean install and Android build

**Files:** none (build step)

- [ ] **Step 1: Delete node_modules and reinstall**

```bash
cd E:\rtmusicbox\mobile
rm -rf node_modules
npm install
```

Expected: npm install completes with no peer dependency errors. If there are warnings about `react-native-track-player`, they are fine as long as there are no errors.

- [ ] **Step 2: Clean Android build cache**

```powershell
# Run via PowerShell script
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
$env:PATH = "$env:LOCALAPPDATA\Android\Sdk\platform-tools;" + $env:PATH
Set-Location "E:\rtmusicbox\mobile\android"
.\gradlew.bat clean
```

Expected output ends with: `BUILD SUCCESSFUL`

- [ ] **Step 3: Build and install on device**

Make sure the Redmi device is connected (USB debugging on), then:

```powershell
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
$env:PATH = "$env:LOCALAPPDATA\Android\Sdk\platform-tools;" + $env:PATH
Set-Location "E:\rtmusicbox\mobile\android"
.\gradlew.bat app:installDebug -x lint -PreactNativeDevServerPort=8081
```

Expected: `BUILD SUCCESSFUL` + `Installing APK 'debug' on device`

- [ ] **Step 4: Start Metro bundler**

Open a new terminal window and run:

```bash
cd E:\rtmusicbox\mobile
npx react-native start --reset-cache
```

Keep this running while testing.

- [ ] **Step 5: Launch app on device**

```bash
"$HOME/AppData/Local/Android/Sdk/platform-tools/adb.exe" -s b61d4b08 shell am start -n "com.radiotedumobile/.MainActivity"
```

---

### Task 6: Verify

**Files:** none (manual verification)

- [ ] **Step 1: Check all 4 tabs open without crash**
  - Radio tab: opens, channel list visible
  - Podcasts tab: opens, episode list visible
  - Jukebox tab: opens
  - Leaderboard tab: opens

- [ ] **Step 2: Check audio playback**
  - Tap a radio channel → stream starts playing
  - MiniPlayer appears at bottom
  - Play/pause works

- [ ] **Step 3: Check Metro log for JS errors**
  - In the Metro terminal, no red errors should appear during normal navigation

- [ ] **Step 4: Final commit**

```bash
cd E:\rtmusicbox
git add -A
git commit -m "chore(mobile): complete RN 0.73 → 0.76.9 upgrade

- React Native 0.73.0 → 0.76.9
- React 18.2.0 → 18.3.2
- Kotlin 1.8.0 → 1.9.24
- AGP 8.1.4 → 8.3.2
- Gradle 8.3 → 8.6
- NDK 25.1 → 26.1
- Removed Flipper (deprecated)
- Removed UIManager.setLayoutAnimationEnabledExperimental
- New Architecture remains disabled (newArchEnabled=false)"
```
